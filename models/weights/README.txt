# This file ensures the weights directory is tracked.
# Place your trained model weights file here as: retinaguard_weights.h5
#
# If no weights file is found, RetinaGuard AI will automatically
# operate in Demo Mode with deterministic stub predictions.
#
# To train the model:
#   python train.py --dataset /path/to/dataset --epochs 40 --batch-size 32
